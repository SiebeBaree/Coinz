/* eslint-disable no-unused-vars */
export enum SubscriptionStatus {
    OnTrial = 'on_trial',
    Active = 'active',
    Paused = 'paused',
    PastDue = 'past_due',
    Unpaid = 'unpaid',
    Cancelled = 'cancelled',
    Expired = 'expired',
}
